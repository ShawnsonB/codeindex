[project]
name = "codeindex"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "chromadb>=0.6",
    "mcp>=1.0",
    "watchdog>=4.0",
]

[tool.setuptools]
py-modules = ["server", "indexer", "watcher"]
